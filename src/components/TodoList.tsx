import styled from '@emotion/styled'

// export const TodoList = styled.ul({
//   width: '100%',
//   listStyle: 'none',
// })

// Or keeping a CSS syntax:
export const TodoList = styled.ul`
  width: 100%;
  list-style: square;
`
